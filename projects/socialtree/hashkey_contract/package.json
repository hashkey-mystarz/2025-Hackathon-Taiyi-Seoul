{
	"name": "socialtree-contract",
	"version": "1.0.0",
	"description": "SocialTree recursive commission smart contract",
	"main": "index.js",
	"scripts": {
		"compile": "hardhat compile",
		"test": "hardhat test",
		"deploy:testnet": "hardhat run scripts/deploy.js --network hashkeyTestnet",
		"deploy:mainnet": "hardhat run scripts/deploy.js --network hashkeyMainnet"
	},
	"dependencies": {
		"@nomicfoundation/hardhat-toolbox": "^3.0.0",
		"dotenv": "^16.3.1"
	},
	"devDependencies": {
		"hardhat": "^2.17.1",
		"ethers": "^6.7.0"
	}
}
